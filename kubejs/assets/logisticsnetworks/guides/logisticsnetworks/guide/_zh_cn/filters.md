---
navigation:
  title: 过滤器
  parent: index.md
  position: 7
---

# 过滤器

过滤器从传输的两端读取。

对于支持的通道类型，传输检查：

1. 源节点上的导出通道过滤器槽位
2. 目标节点上的导入通道过滤器槽位

因此，过滤器在实践中是双向的。

通道过滤模式改变白名单行为：

1. 任意意味着至少一个白名单过滤器可以匹配。
2. 全部意味着所有白名单过滤器都必须匹配。
3. 黑名单匹配始终阻止。

打开下面的每个过滤器页面以了解确切行为。

<SubPages icons={true} />